# Optional real-hardware transport.
# The base Gemini SDK already installs a compatible modern websockets release;
# this file makes the real-mode requirement explicit.
-r requirements.txt
websockets>=13.0,<17.0
